# This file was automatically generated.

[package]
edition = "2024"
name = "AutonoTorpedo-V2_2"
version = "0.1.0"

[workspace]
resolver = "2"

[dependencies]
cortex-m = { version = "0.7.7", features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = "0.7.5"
defmt = { version = "1.1.1", optional = true }
defmt-rtt = { version = "1.3.0", optional = true }
embassy-futures = "0.1.2"
embassy-sync = "0.8.0"
embassy-time = { version = "0.5.1", features = ["tick-hz-32_768"] }
panic-halt = "1.0.0"
panic-probe = { version = "1.0.0", features = ["print-defmt"], optional = true }
embassy-executor = { version = "0.10.0", features = ["platform-cortex-m", "executor-thread", "defmt"] }
embassy-stm32 = { version = "0.6.0", features = ["defmt", "time-driver-any", "stm32g473rc", "memory-x", "unstable-pac", "exti", "single-bank"] }
paste = "1.0.15"
icm426xx = { version = "0.4.0", features = ["async", "defmt"] }
#mmc5983_rs = { version = "0.0.1", features = ["async"] }
embedded-hal-async = "1.0.0"
embedded-hal-bus = { version = "0.3.0", features = ["async"] }
embassy-embedded-hal = "0.6.0"
static_cell = "2.1.1"
ssd1306 = {version = "0.10.0", features = ["async"]}
embedded-graphics = { version = "0.8.2", features = ["defmt"] }
bytemuck = { version = ">=1.16.0,<2.0.0", features = ["derive"] }
ms5837 = { path = "libs/ms5837" }
mmc5983_rs = { path = "libs/MMC5983", features = ["async"] }
heapless = { version = "0.9.1", features = ["defmt"] }

[[bin]]
name = "AutonoTorpedo-V2_2"
test = false
bench = false

[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

[features]
defmt = ["dep:defmt"]
defmt-rtt = ["dep:defmt-rtt"]
panic-probe = ["dep:panic-probe"]
default = ["debug"]
debug = [
    "defmt",
    "defmt-rtt",
    "panic-probe",
    "embassy-executor/defmt",
    "embassy-sync/defmt",
    "embassy-futures/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
]
